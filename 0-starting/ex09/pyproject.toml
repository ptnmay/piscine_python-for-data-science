requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "A simple Python package"
readme = "README.md"
requires-python = ">=3.7"
authors = [
    { name = "psaeyang" }
]
license = { text = "MIT" }

[tool.setuptools]
packages = ["ft_package"]